<h1>Registration Details</h1>

<p><strong>Event:</strong> <%= @registration.event.name %></p>
<p><strong>Attendee:</strong> <%= @registration.attendee_name %></p>
<p><strong>Email:</strong> <%= @registration.attendee_email %></p>
<p><strong>Registered At:</strong> <%= @registration.created_at.strftime("%B %d, %Y %H:%M") %></p>

<%= link_to "Edit", edit_admin_registration_path(@registration) %> |
<%= link_to "Delete", admin_registration_path(@registration), method: :delete, data: { confirm: "Are you sure?" } %> |
<%= link_to "Back", admin_registrations_path %>
