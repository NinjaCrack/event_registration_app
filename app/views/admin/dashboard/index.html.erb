<h1>Admin Dashboard</h1>
<p>Total events: <%= @total_events %></p>
<p>Total attendees: <%= @total_attendees %></p>

<h2>Top events by attendees</h2>
<ul>
  <% @recent_events.each do |e| %>
    <li><%= link_to e.name, admin_event_path(e) %> — <%= e.attributes['attendees_count'] %> attendees</li>
  <% end %>
</ul>
